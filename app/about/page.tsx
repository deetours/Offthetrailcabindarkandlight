import { AboutPage } from "@/components/about/about-page"

export const metadata = {
  title: "About | Offthetrail",
  description: "We believe travel is about people, not places. About belonging, not just visiting.",
}

export default function About() {
  return <AboutPage />
}
